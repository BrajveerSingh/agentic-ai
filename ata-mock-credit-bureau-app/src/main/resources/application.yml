spring:
  application:
    name: ata-mock-credit-bureau

server:
  port: 8080

# Enable the mock controller from the ata-mcp library.
mcp:
  mock:
    credit-bureau:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

