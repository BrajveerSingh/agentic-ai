spring:
  application:
    name: ata-gateway

server:
  port: 8080

ata:
  orchestrator:
    base-url: ${ATA_ORCHESTRATOR_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

